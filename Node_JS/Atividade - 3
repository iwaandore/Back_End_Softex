const express = require('express')
const app = express()
const port = 8080

app.get('/', (req, res) => {
    res.send('Olá,Mundo')
})

app.post('/', (req, res) => {
    res.send('Recebi o seu Olá :)')
})

app.listen(port, () => {
    console.log('Rodando na porta:' + port)
})
